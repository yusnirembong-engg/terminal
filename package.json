{
  "name": "telebot-control-panel",
  "version": "1.0.0",
  "description": "Secure Telegram Bot Control Panel",
  "main": "src/index.js",
  "scripts": {
    "dev": "netlify dev",
    "build": "mkdir -p dist && cp -r src/public/* dist/",
    "prebuild": "npm install",
    "postbuild": "echo 'Build completed successfully'",
    "start": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.0",
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "@netlify/functions": "^1.6.0",
    "netlify-cli": "^15.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
